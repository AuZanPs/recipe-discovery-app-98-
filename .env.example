# Environment configuration for Recipe Discovery App
# Copy this file to `.env` and adjust values as needed.
# Vite exposes only vars prefixed with VITE_

# TheMealDB API key ("1" is public test key; replace with your own if you get a paid key)
VITE_MEALDB_API_KEY=1

# Optionally override full base URL (leave commented to use default composed from key above)
# Example: https://www.themealdb.com/api/json/v1/1
# VITE_MEALDB_BASE_URL=https://www.themealdb.com/api/json/v1/1
